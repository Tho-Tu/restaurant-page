import homePage from "./homePage.js";
import "./style.css";

console.log("index.js");
// loads the default home page first
homePage();

function loadPage() {
  // 4 event listeners for each menu link: home, menu, about us, reservation
  // clear the page
  // load the page depending on user click
}
